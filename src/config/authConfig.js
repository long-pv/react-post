import { apiConfig } from './apiConfig';

export const authConfig = {
    loginPath: apiConfig.auth.loginPath,
    registerPath: apiConfig.auth.registerPath,
    logoutPath: apiConfig.auth.logoutPath,
};
